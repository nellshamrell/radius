# radius Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-19

## Active Technologies

- Go 1.26 (aligned with the repository's `go.mod`) + `github.com/spf13/cobra` (CLI framework, already used by `rad` CLI), `encoding/json` (Aspire manifest parsing), `text/template` (Bicep text generation), standard library only for core logic (001-aspire-manifest-translation)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Go 1.26 (aligned with the repository's `go.mod`)

## Code Style

Go 1.26 (aligned with the repository's `go.mod`): Follow standard conventions

## Recent Changes

- 001-aspire-manifest-translation: Added Go 1.26 (aligned with the repository's `go.mod`) + `github.com/spf13/cobra` (CLI framework, already used by `rad` CLI), `encoding/json` (Aspire manifest parsing), `text/template` (Bicep text generation), standard library only for core logic

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
